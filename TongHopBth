local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")

local player = Players.LocalPlayer
-- Tối ưu tốc độ lấy Character để script load ngay lập tức không bị delay
local char = player.Character or player.CharacterInstance or nil
local humanoid = char and char:FindFirstChildOfClass("Humanoid") or nil

-- ==================== HỆ THỐNG BIẾN QUẢN LÝ TÍNH NĂNG ====================
local noclip = false
local flying = false
local infJump = false
local espEnabled = false
local npcEspEnabled = false
local itemEspEnabled = false 
local fullbright = false
local antiStun = false
local antiDamage = false
local freeCamEnabled = false
local fixLagEnabled = false 

local FlySpeed = 50
local CustomSpeed = 16
local CustomJump = 50
local MenuScale = 1.0 

local flyBodyGyro = nil
local flyBodyVelocity = nil

local originalMaterials = {}
local originalLighting = {
	GlobalShadows = Lighting.GlobalShadows,
	Technology = Lighting.Technology
}

local ThemeColor = Color3.fromRGB(255, 50, 50) 
local ObjectsToTheme = {}

local function UpdateTheme(newColor)
	ThemeColor = newColor
	for obj, prop in pairs(ObjectsToTheme) do
		if obj and obj.Parent then
			obj[prop] = newColor
		else
			ObjectsToTheme[obj] = nil
		end
	end
end

player.CharacterAdded:Connect(function(newChar)
	char = newChar
	humanoid = newChar:WaitForChild("Humanoid", 3)
	flying = false
	noclip = false
	if flyBodyGyro then flyBodyGyro:Destroy() flyBodyGyro = nil end
	if flyBodyVelocity then flyBodyVelocity:Destroy() flyBodyVelocity = nil end
end)

-- ==================== HỆ THỐNG ANTI-BAN (BYPASS ANTI-CHEAT) ====================
local function InitiateAntiBan()
	local gmt = getrawmetatable(game)
	if gmt and setreadonly then
		setreadonly(gmt, false)
		local oldNamecall = gmt.__namecall
		local oldIndex = gmt.__index

		gmt.__namecall = newcclosure(function(self, ...)
			local method = getnamecallmethod()
			if not checkcaller() then
				if method == "Kick" or method == "kick" then return nil
				elseif method == "FireServer" and self.Name:lower():find("cheat") then return nil end
			end
			return oldNamecall(self, ...)
		end)

		gmt.__index = newcclosure(function(self, property)
			if not checkcaller() and self:IsA("Humanoid") then
				if property == "WalkSpeed" then return 16
				elseif property == "JumpPower" then return 50
				elseif property == "PlatformStand" then return false end
			end
			return oldIndex(self, property)
		end)
		setreadonly(gmt, true)
	end
end
task.spawn(function() pcall(InitiateAntiBan) end) -- Chạy ngầm để không cản trở UI load

-- ==================== HỆ THỐNG GIAO DIỆN (GUI) ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NguyenDzHub_Menu_v4_0"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

----------------------------------------------------------------
-- [NEW] HỆ THỐNG THÔNG BÁO THÀNH TỰU (ACHIEVEMENT NOTIFICATION)
----------------------------------------------------------------
local function ShowAchievementNotify(title, desc)
	local NotifyFrame = Instance.new("Frame")
	NotifyFrame.Size = UDim2.new(0, 260, 0, 65)
	-- Vị trí xuất phát ẩn ngoài rìa màn hình bên phải góc dưới
	NotifyFrame.Position = UDim2.new(1, 30, 1, -85)
	NotifyFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
	NotifyFrame.BackgroundTransparency = 0.15
	NotifyFrame.BorderSizePixel = 0
	NotifyFrame.Parent = ScreenGui
	Instance.new("UICorner", NotifyFrame).CornerRadius = UDim.new(0, 8)

	-- Thanh màu trang trí cạnh trái thông báo
	local SideLine = Instance.new("Frame")
	SideLine.Size = UDim2.new(0, 4, 1, 0)
	SideLine.BackgroundColor3 = ThemeColor
	SideLine.BorderSizePixel = 0
	SideLine.Parent = NotifyFrame
	Instance.new("UICorner", SideLine).CornerRadius = UDim.new(0, 8)
	ObjectsToTheme[SideLine] = "BackgroundColor3"

	-- Icon giả lập thành tựu dạng tròn
	local IconBox = Instance.new("Frame")
	IconBox.Size = UDim2.new(0, 35, 0, 35)
	IconBox.Position = UDim2.new(0, 15, 0.5, -17)
	IconBox.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
	IconBox.BorderSizePixel = 0
	IconBox.Parent = NotifyFrame
	Instance.new("UICorner", IconBox).CornerRadius = UDim.new(0, 18)

	local IconText = Instance.new("TextLabel")
	IconText.Size = UDim2.new(1, 0, 1, 0)
	IconText.BackgroundTransparency = 1
	IconText.Text = "🏆"
	IconText.TextSize = 18
	IconText.Parent = IconBox

	-- Tiêu đề thông báo
	local TitleLabel = Instance.new("TextLabel")
	TitleLabel.Size = UDim2.new(1, -70, 0, 20)
	TitleLabel.Position = UDim2.new(0, 60, 0, 12)
	TitleLabel.BackgroundTransparency = 1
	TitleLabel.Text = title
	TitleLabel.TextColor3 = Color3.fromRGB(255, 215, 0) -- Màu vàng Gold quý phái
	TitleLabel.Font = Enum.Font.SourceSansBold
	TitleLabel.TextSize = 14
	TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
	TitleLabel.Parent = NotifyFrame

	-- Nội dung mô tả chi tiết
	local DescLabel = Instance.new("TextLabel")
	DescLabel.Size = UDim2.new(1, -70, 0, 20)
	DescLabel.Position = UDim2.new(0, 60, 0, 28)
	DescLabel.BackgroundTransparency = 1
	DescLabel.Text = desc
	DescLabel.TextColor3 = Color3.fromRGB(220, 220, 225)
	DescLabel.Font = Enum.Font.SourceSans
	DescLabel.TextSize = 13
	DescLabel.TextXAlignment = Enum.TextXAlignment.Left
	DescLabel.Parent = NotifyFrame

	-- Hiệu ứng trượt mượt mà đi vào từ bên phải
	NotifyFrame:TweenPosition(UDim2.new(1, -280, 1, -85), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.5, true)

	-- Tự động thu hồi và biến mất sau 4 giây
	task.delay(4, function()
		if NotifyFrame then
			local closeTween = TweenService:Create(NotifyFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
				Position = UDim2.new(1, 30, 1, -85),
				BackgroundTransparency = 1
			})
			closeTween:Play()
			closeTween.Completed:Connect(function() NotifyFrame:Destroy() end)
		end
	end)
end

-- Gọi thông báo ngay lập tức khi chạy script
ShowAchievementNotify("Mở khóa Thành Tựu!", "NguyenDzHub v4.0 đã sẵn sàng.")

----------------------------------------------------------------
-- [2] CỬA SỔ CÂU HỎI BẤT BUỘC
----------------------------------------------------------------
local QuestionFrame = Instance.new("Frame")
QuestionFrame.Parent = ScreenGui
QuestionFrame.Size = UDim2.new(0, 320, 0, 160)
QuestionFrame.Position = UDim2.new(0.5, -160, 0.5, -80)
QuestionFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
QuestionFrame.BackgroundTransparency = 0.15 
Instance.new("UICorner", QuestionFrame).CornerRadius = UDim.new(0, 10)

local Question = Instance.new("TextLabel")
Question.Parent = QuestionFrame
Question.Size = UDim2.new(1, 0, 0, 60)
Question.BackgroundTransparency = 1
Question.TextScaled = true
Question.TextColor3 = Color3.new(1, 1, 1)
Question.Text = "Nguyên có đẹp trai sigma ngầu lòi k?"
Question.Font = Enum.Font.SourceSansBold

local Btn1 = Instance.new("TextButton")
Btn1.Parent = QuestionFrame
Btn1.Size = UDim2.new(0, 120, 0, 40)
Btn1.Position = UDim2.new(0, 25, 0, 95)
Btn1.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
Btn1.TextColor3 = Color3.new(1, 1, 1)
Btn1.Font = Enum.Font.SourceSansBold
Btn1.TextSize = 18
Btn1.Text = "Có"
Instance.new("UICorner", Btn1).CornerRadius = UDim.new(0, 6)

local Btn2 = Instance.new("TextButton")
Btn2.Parent = QuestionFrame
Btn2.Size = UDim2.new(0, 120, 0, 40)
Btn2.Position = UDim2.new(0, 175, 0, 95)
Btn2.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
Btn2.TextColor3 = Color3.new(1, 1, 1)
Btn2.Font = Enum.Font.SourceSansBold
Btn2.TextSize = 18
Btn2.Text = "Có"
Instance.new("UICorner", Btn2).CornerRadius = UDim.new(0, 6)

----------------------------------------------------------------
-- [3] MENU CHÍNH
----------------------------------------------------------------
local MainMenu = Instance.new("Frame")
MainMenu.Parent = ScreenGui
MainMenu.Size = UDim2.new(0, 560, 0, 360)
MainMenu.Position = UDim2.new(0.5, -280, 0.5, -180)
MainMenu.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainMenu.BackgroundTransparency = 0.15 
MainMenu.Active = true
MainMenu.Draggable = true 
MainMenu.Visible = false
Instance.new("UICorner", MainMenu).CornerRadius = UDim.new(0, 12)

local BaseMenuSize = Vector2.new(560, 360)

local function ChangeMenuScale(scaleValue)
	MenuScale = math.clamp(scaleValue, 0.6, 1.5)
	local newWidth = BaseMenuSize.X * MenuScale
	local newHeight = BaseMenuSize.Y * MenuScale
	MainMenu.Size = UDim2.new(0, newWidth, 0, newHeight)
	
	for _, child in ipairs(MainMenu:GetDescendants()) do
		if child:IsA("TextLabel") or child:IsA("TextButton") or child:IsA("TextBox") then
			if not child:GetAttribute("BaseTextSize") then child:SetAttribute("BaseTextSize", child.TextSize) end
			local baseSize = child:GetAttribute("BaseTextSize")
			child.TextSize = math.clamp(math.round(baseSize * MenuScale), 8, 32)
		end
	end
end

local TopBar = Instance.new("Frame")
TopBar.Parent = MainMenu
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundTransparency = 1

local TitleText = Instance.new("TextLabel")
TitleText.Parent = TopBar
TitleText.Size = UDim2.new(1, -90, 1, 0)
TitleText.Position = UDim2.new(0, 15, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "NguyenDzHub : Nguyên Sigma Edition v4.0" 
TitleText.TextColor3 = Color3.fromRGB(240, 240, 245)
TitleText.Font = Enum.Font.SourceSansBold
TitleText.TextSize = 15
TitleText.TextXAlignment = Enum.TextXAlignment.Left

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Parent = TopBar
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -70, 0, 5)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.TextColor3 = Color3.fromRGB(160, 160, 170)
MinimizeBtn.Text = "—"
MinimizeBtn.Font = Enum.Font.SourceSansBold
MinimizeBtn.TextSize = 16

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.TextColor3 = Color3.fromRGB(255, 75, 75) 
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 16

local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Parent = MainMenu
Sidebar.Size = UDim2.new(0, 160, 1, -50)
Sidebar.Position = UDim2.new(0, 10, 0, 45)
Sidebar.BackgroundTransparency = 1
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 400)
Sidebar.ScrollBarThickness = 0

local SidebarLayout = Instance.new("UIListLayout")
SidebarLayout.Parent = Sidebar
SidebarLayout.Padding = UDim.new(0, 5)

local Divider = Instance.new("Frame")
Divider.Parent = MainMenu
Divider.Size = UDim2.new(0, 1, 1, -60)
Divider.Position = UDim2.new(0, 175, 0, 50)
Divider.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
Divider.BorderSizePixel = 0

local ContentContainer = Instance.new("Frame")
ContentContainer.Parent = MainMenu
ContentContainer.Size = UDim2.new(1, -190, 1, -55)
ContentContainer.Position = UDim2.new(0, 185, 0, 45)
ContentContainer.BackgroundTransparency = 1

local Tabs = {}
local TabButtons = {}

local function CreateTab(tabName)
	local TabPage = Instance.new("ScrollingFrame")
	TabPage.Size = UDim2.new(1, 0, 1, 0)
	TabPage.BackgroundTransparency = 1
	TabPage.CanvasSize = UDim2.new(0, 0, 0, 600)
	TabPage.ScrollBarThickness = 2
	TabPage.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 65)
	TabPage.Visible = false
	TabPage.Parent = ContentContainer

	local PageLayout = Instance.new("UIListLayout")
	PageLayout.Parent = TabPage
	PageLayout.Padding = UDim.new(0, 8)
	
	local TabBtn = Instance.new("TextButton")
	TabBtn.Size = UDim2.new(1, -5, 0, 35)
	TabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
	TabBtn.BackgroundTransparency = 1
	TabBtn.TextColor3 = Color3.fromRGB(160, 160, 165)
	TabBtn.Text = "  " .. tabName
	TabBtn.Font = Enum.Font.SourceSansBold
	TabBtn.TextSize = 14
	TabBtn.TextXAlignment = Enum.TextXAlignment.Left
	TabBtn.Parent = Sidebar
	Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 6)

	Tabs[tabName] = TabPage
	TabButtons[tabName] = TabBtn

	TabBtn.MouseButton1Click:Connect(function()
		for _, page in pairs(Tabs) do page.Visible = false end
		for _, btn in pairs(TabButtons) do 
			btn.BackgroundTransparency = 1 
			btn.TextColor3 = Color3.fromRGB(160, 160, 165)
		end
		TabPage.Visible = true
		TabBtn.BackgroundTransparency = 0.4
		TabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	end)

	return TabPage
end

local function CreateSectionTitle(page, titleText)
	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, 0, 0, 25)
	Label.BackgroundTransparency = 1
	Label.Text = titleText
	Label.TextColor3 = Color3.fromRGB(255, 255, 255)
	Label.Font = Enum.Font.SourceSansBold
	Label.TextSize = 16
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = page
end

local SyncUINoclip, SyncUIFly

local function CreateToggleRow(page, featureText, defaultState, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 40)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.BorderSizePixel = 0
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.7, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local ToggleBtn = Instance.new("TextButton")
	ToggleBtn.Size = UDim2.new(0, 45, 0, 22)
	ToggleBtn.Position = UDim2.new(1, -55, 0, 9)
	ToggleBtn.BackgroundColor3 = defaultState and ThemeColor or Color3.fromRGB(50, 50, 55)
	ToggleBtn.Text = ""
	ToggleBtn.Parent = Row
	Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 11)
	if defaultState then ObjectsToTheme[ToggleBtn] = "BackgroundColor3" end

	local StateCircle = Instance.new("Frame")
	StateCircle.Size = UDim2.new(0, 16, 0, 16)
	StateCircle.Position = defaultState and UDim2.new(1, -20, 0, 3) or UDim2.new(0, 4, 0, 3)
	StateCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	StateCircle.Parent = ToggleBtn
	Instance.new("UICorner", StateCircle).CornerRadius = UDim.new(0, 8)

	local active = defaultState
	
	local function updateVisuals()
		if active then
			ObjectsToTheme[ToggleBtn] = "BackgroundColor3"
			TweenService:Create(ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = ThemeColor}):Play()
			TweenService:Create(StateCircle, TweenInfo.new(0.15), {Position = UDim2.new(1, -20, 0, 3)}):Play()
		else
			ObjectsToTheme[ToggleBtn] = nil
			TweenService:Create(ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(50, 50, 55)}):Play()
			TweenService:Create(StateCircle, TweenInfo.new(0.15), {Position = UDim2.new(0, 4, 0, 3)}):Play()
		end
	end

	ToggleBtn.MouseButton1Click:Connect(function()
		active = not active
		callback(active)
		updateVisuals()
	end)
	
	return function(forceState)
		active = forceState
		callback(active)
		updateVisuals()
	end
end

local function CreateInputRow(page, featureText, defaultText, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 40)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.BorderSizePixel = 0
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.5, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local BoxInput = Instance.new("TextBox")
	BoxInput.Size = UDim2.new(0, 100, 0, 26)
	BoxInput.Position = UDim2.new(1, -112, 0, 7)
	BoxInput.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
	BoxInput.BorderSizePixel = 0
	BoxInput.TextColor3 = Color3.fromRGB(0, 255, 200)
	BoxInput.Text = defaultText
	BoxInput.Font = Enum.Font.SourceSansBold
	BoxInput.TextSize = 14
	BoxInput.ClearTextOnFocus = true
	BoxInput.Parent = Row
	Instance.new("UICorner", BoxInput).CornerRadius = UDim.new(0, 4)

	BoxInput.FocusLost:Connect(function(enter)
		callback(BoxInput.Text, enter)
	end)
end

local function CreateSliderRow(page, featureText, min, max, default, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 45)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.4, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText .. " (" .. string.format("%.1f", default) .. "x)"
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local SliderContainer = Instance.new("Frame")
	SliderContainer.Size = UDim2.new(0, 120, 0, 6)
	SliderContainer.Position = UDim2.new(1, -140, 0, 20)
	SliderContainer.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
	SliderContainer.Parent = Row
	Instance.new("UICorner", SliderContainer)

	local SliderFill = Instance.new("Frame")
	SliderFill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	SliderFill.BackgroundColor3 = ThemeColor
	SliderFill.Parent = SliderContainer
	Instance.new("UICorner", SliderFill)
	ObjectsToTheme[SliderFill] = "BackgroundColor3"

	local SliderButton = Instance.new("TextButton")
	SliderButton.Size = UDim2.new(0, 14, 0, 14)
	SliderButton.Position = UDim2.new((default - min) / (max - min), -7, 0, -4)
	SliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	SliderButton.Text = ""
	SliderButton.Parent = SliderContainer
	Instance.new("UICorner", SliderButton).CornerRadius = UDim.new(0, 7)

	local isSliding = false

	local function UpdateSlider(inputPosition)
		local percentage = math.clamp((inputPosition.X - SliderContainer.AbsolutePosition.X) / SliderContainer.AbsoluteSize.X, 0, 1)
		SliderFill.Size = UDim2.new(percentage, 0, 1, 0)
		SliderButton.Position = UDim2.new(percentage, -7, 0, -4)
		
		local realValue = min + (percentage * (max - min))
		Label.Text = featureText .. " (" .. string.format("%.1f", realValue) .. "x)"
		callback(realValue)
	end

	SliderButton.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isSliding = true end
	end)
	UIS.InputChanged:Connect(function(input)
		if isSliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then UpdateSlider(input.Position) end
	end)
	UIS.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isSliding = false end
	end)
end

local function CreateButtonRow(page, btnText, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 45)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.BorderSizePixel = 0
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Btn = Instance.new("TextButton")
	Btn.Size = UDim2.new(1, -20, 0, 33)
	Btn.Position = UDim2.new(0, 10, 0, 6)
	Btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
	Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	Btn.Text = btnText
	Btn.Font = Enum.Font.SourceSansBold
	Btn.TextSize = 14
	Btn.Parent = Row
	Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)

	Btn.MouseButton1Click:Connect(callback)
end

-- ==================== KHỞI TẠO CÁC TAB CHỨC NĂNG ====================
local MainTab = CreateTab("Main Hacks")
local StatusTab = CreateTab("Status/Configs")
local UtilityTab = CreateTab("Utility")
local InfoT
