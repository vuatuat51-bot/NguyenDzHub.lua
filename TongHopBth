local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local TeleportService = game:GetService("TeleportService")

local player = Players.LocalPlayer
local char = nil
local humanoid = nil

-- Tải nhân vật ngầm chạy song song
task.spawn(function()
	char = player.Character or player.CharacterAdded:Wait()
	humanoid = char:WaitForChild("Humanoid", 5) or char:FindFirstChildOfClass("Humanoid")
end)

-- ==================== HỆ THỐNG BIẾN TÍNH NĂNG ====================
local noclip = false
local flying = false
local infJump = false
local FlySpeed = 50
local CustomSpeed = 16
local CustomJump = 50
local MenuScale = 1.0 

local flyBodyGyro = nil
local flyBodyVelocity = nil

local ThemeColor = Color3.fromRGB(255, 50, 50) 
local ObjectsToTheme = {}

player.CharacterAdded:Connect(function(newChar)
	char = newChar
	humanoid = newChar:WaitForChild("Humanoid", 5) or newChar:FindFirstChildOfClass("Humanoid")
	flying = false; noclip = false
	if flyBodyGyro then pcall(function() flyBodyGyro:Destroy() end) flyBodyGyro = nil end
	if flyBodyVelocity then pcall(function() flyBodyVelocity:Destroy() end) flyBodyVelocity = nil end
end)

-- ==================== KHỞI TẠO GUI GỐC ====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NguyenDzHub_v4_4"
ScreenGui.ResetOnSpawn = false
pcall(function() ScreenGui.Parent = player:WaitForChild("PlayerGui", 5) or player.PlayerGui end)

-- Hàm kéo thả giao diện mượt chống bug click
local function MakeDraggable(gui)
	local dragging, dragInput, dragStart, startPos
	gui.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true; dragStart = input.Position; startPos = gui.Position
			input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
		end
	end)
	gui.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
	end)
	UIS.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

----------------------------------------------------------------
-- [1] BẢNG HIỆU ỨNG CHỮ "NGUYÊN SIGMA ĐẸP TRAI" (TỰ BIẾN MẤT)
----------------------------------------------------------------
local SigmaFrame = Instance.new("Frame")
SigmaFrame.Parent = ScreenGui
SigmaFrame.Size = UDim2.new(0, 360, 0, 90)
SigmaFrame.Position = UDim2.new(0.5, -180, -0.2, 0) -- Xuất phát từ trên đỉnh màn hình rơi xuống
SigmaFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
SigmaFrame.BackgroundTransparency = 0.1
SigmaFrame.BorderSizePixel = 0
Instance.new("UICorner", SigmaFrame).CornerRadius = UDim.new(0, 10)

-- Viền phát sáng nhẹ cho chuẩn Sigma
local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 215, 0) -- Màu vàng hoàng gia / gold
UIStroke.Thickness = 2
UIStroke.Parent = SigmaFrame

local SigmaText = Instance.new("TextLabel")
SigmaText.Parent = SigmaFrame
SigmaText.Size = UDim2.new(1, 0, 1, 0)
SigmaText.BackgroundTransparency = 1
SigmaText.Text = "👑 NGUYÊN SIGMA ĐẸP TRAI NGẦU LÒI 👑"
SigmaText.TextColor3 = Color3.fromRGB(255, 255, 255)
SigmaText.Font = Enum.Font.SourceSansBold
SigmaText.TextSize = 18
SigmaText.TextWrapped = true

----------------------------------------------------------------
-- [2] MENU CHÍNH
----------------------------------------------------------------
local MainMenu = Instance.new("Frame")
MainMenu.Parent = ScreenGui
MainMenu.Size = UDim2.new(0, 560, 0, 360)
MainMenu.Position = UDim2.new(0.5, -280, 0.5, -180)
MainMenu.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainMenu.BackgroundTransparency = 0.15 
MainMenu.Active = true
MainMenu.Visible = false -- Ban đầu ẩn đi để đợi bảng Sigma chạy xong
Instance.new("UICorner", MainMenu).CornerRadius = UDim.new(0, 12)
MakeDraggable(MainMenu)

local BaseMenuSize = Vector2.new(560, 360)
local function ChangeMenuScale(scaleValue)
	MenuScale = math.clamp(scaleValue, 0.6, 1.5)
	MainMenu.Size = UDim2.new(0, BaseMenuSize.X * MenuScale, 0, BaseMenuSize.Y * MenuScale)
end

local TopBar = Instance.new("Frame")
TopBar.Parent = MainMenu
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundTransparency = 1

local TitleText = Instance.new("TextLabel")
TitleText.Parent = TopBar
TitleText.Size = UDim2.new(1, -90, 1, 0)
TitleText.Position = UDim2.new(0, 15, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "NguyenDzHub : Nguyên Sigma Edition v4.4" 
TitleText.TextColor3 = Color3.fromRGB(240, 240, 245)
TitleText.Font = Enum.Font.SourceSansBold
TitleText.TextSize = 15
TitleText.TextXAlignment = Enum.TextXAlignment.Left

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Parent = TopBar
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -70, 0, 5)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.TextColor3 = Color3.fromRGB(160, 160, 170)
MinimizeBtn.Text = "—"
MinimizeBtn.Font = Enum.Font.SourceSansBold
MinimizeBtn.TextSize = 16

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.BackgroundTransparency = 1
CloseBtn.TextColor3 = Color3.fromRGB(255, 75, 75) 
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 16

local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Parent = MainMenu
Sidebar.Size = UDim2.new(0, 160, 1, -50)
Sidebar.Position = UDim2.new(0, 10, 0, 45)
Sidebar.BackgroundTransparency = 1
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 400)
Sidebar.ScrollBarThickness = 0
Instance.new("UIListLayout", Sidebar).Padding = UDim.new(0, 5)

local Divider = Instance.new("Frame")
Divider.Parent = MainMenu
Divider.Size = UDim2.new(0, 1, 1, -60)
Divider.Position = UDim2.new(0, 175, 0, 50)
Divider.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
Divider.BorderSizePixel = 0

local ContentContainer = Instance.new("Frame")
ContentContainer.Parent = MainMenu
ContentContainer.Size = UDim2.new(1, -190, 1, -55)
ContentContainer.Position = UDim2.new(0, 185, 0, 45)
ContentContainer.BackgroundTransparency = 1

local Tabs = {}
local TabButtons = {}

local function CreateTab(tabName)
	local TabPage = Instance.new("ScrollingFrame")
	TabPage.Size = UDim2.new(1, 0, 1, 0)
	TabPage.BackgroundTransparency = 1
	TabPage.CanvasSize = UDim2.new(0, 0, 0, 550)
	TabPage.ScrollBarThickness = 2
	TabPage.Visible = false
	TabPage.Parent = ContentContainer
	Instance.new("UIListLayout", TabPage).Padding = UDim.new(0, 8)
	
	local TabBtn = Instance.new("TextButton")
	TabBtn.Size = UDim2.new(1, -5, 0, 35)
	TabBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
	TabBtn.BackgroundTransparency = 1
	TabBtn.TextColor3 = Color3.fromRGB(160, 160, 165)
	TabBtn.Text = "  " .. tabName
	TabBtn.Font = Enum.Font.SourceSansBold
	TabBtn.TextSize = 14
	TabBtn.TextXAlignment = Enum.TextXAlignment.Left
	TabBtn.Parent = Sidebar
	Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 6)

	Tabs[tabName] = TabPage
	TabButtons[tabName] = TabBtn

	TabBtn.MouseButton1Click:Connect(function()
		for _, page in pairs(Tabs) do page.Visible = false end
		for _, btn in pairs(TabButtons) do btn.BackgroundTransparency = 1; btn.TextColor3 = Color3.fromRGB(160, 160, 165) end
		TabPage.Visible = true
		TabBtn.BackgroundTransparency = 0.4
		TabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	end)
	return TabPage
end

local function CreateSectionTitle(page, titleText)
	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, 0, 0, 25)
	Label.BackgroundTransparency = 1
	Label.Text = titleText
	Label.TextColor3 = Color3.fromRGB(255, 255, 255)
	Label.Font = Enum.Font.SourceSansBold
	Label.TextSize = 16
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = page
end

local SyncUINoclip, SyncUIFly

local function CreateToggleRow(page, featureText, defaultState, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 40)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.7, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local ToggleBtn = Instance.new("TextButton")
	ToggleBtn.Size = UDim2.new(0, 45, 0, 22)
	ToggleBtn.Position = UDim2.new(1, -55, 0, 9)
	ToggleBtn.BackgroundColor3 = defaultState and ThemeColor or Color3.fromRGB(50, 50, 55)
	ToggleBtn.Text = ""
	ToggleBtn.Parent = Row
	Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 11)
	if defaultState then ObjectsToTheme[ToggleBtn] = "BackgroundColor3" end

	local StateCircle = Instance.new("Frame")
	StateCircle.Size = UDim2.new(0, 16, 0, 16)
	StateCircle.Position = defaultState and UDim2.new(1, -20, 0, 3) or UDim2.new(0, 4, 0, 3)
	StateCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	StateCircle.Parent = ToggleBtn
	Instance.new("UICorner", StateCircle).CornerRadius = UDim.new(0, 8)

	local active = defaultState
	local function updateVisuals()
		if active then
			ObjectsToTheme[ToggleBtn] = "BackgroundColor3"
			TweenService:Create(ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = ThemeColor}):Play()
			TweenService:Create(StateCircle, TweenInfo.new(0.15), {Position = UDim2.new(1, -20, 0, 3)}):Play()
		else
			ObjectsToTheme[ToggleBtn] = nil
			TweenService:Create(ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(50, 50, 55)}):Play()
			TweenService:Create(StateCircle, TweenInfo.new(0.15), {Position = UDim2.new(0, 4, 0, 3)}):Play()
		end
	end

	ToggleBtn.MouseButton1Click:Connect(function()
		active = not active
		callback(active)
		updateVisuals()
	end)
	return function(forceState) active = forceState; callback(active); updateVisuals() end
end

local function CreateInputRow(page, featureText, defaultText, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 40)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.5, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local BoxInput = Instance.new("TextBox")
	BoxInput.Size = UDim2.new(0, 100, 0, 26)
	BoxInput.Position = UDim2.new(1, -112, 0, 7)
	BoxInput.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
	BoxInput.BorderSizePixel = 0
	BoxInput.TextColor3 = Color3.fromRGB(0, 255, 200)
	BoxInput.Text = defaultText
	BoxInput.Font = Enum.Font.SourceSansBold
	BoxInput.TextSize = 14
	BoxInput.Parent = Row
	Instance.new("UICorner", BoxInput).CornerRadius = UDim.new(0, 4)
	BoxInput.FocusLost:Connect(function() callback(BoxInput.Text) end)
end

local function CreateSliderRow(page, featureText, min, max, default, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 45)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(0.4, 0, 1, 0)
	Label.Position = UDim2.new(0, 12, 0, 0)
	Label.BackgroundTransparency = 1
	Label.Text = featureText .. " (" .. string.format("%.1f", default) .. "x)"
	Label.TextColor3 = Color3.fromRGB(210, 210, 215)
	Label.Font = Enum.Font.SourceSans
	Label.TextSize = 14
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Row

	local SliderContainer = Instance.new("Frame")
	SliderContainer.Size = UDim2.new(0, 120, 0, 6)
	SliderContainer.Position = UDim2.new(1, -140, 0, 20)
	SliderContainer.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
	SliderContainer.Parent = Row

	local SliderFill = Instance.new("Frame")
	SliderFill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
	SliderFill.BackgroundColor3 = ThemeColor
	SliderFill.Parent = SliderContainer
	ObjectsToTheme[SliderFill] = "BackgroundColor3"

	local SliderButton = Instance.new("TextButton")
	SliderButton.Size = UDim2.new(0, 14, 0, 14)
	SliderButton.Position = UDim2.new((default - min) / (max - min), -7, 0, -4)
	SliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	SliderButton.Text = ""
	SliderButton.Parent = SliderContainer
	Instance.new("UICorner", SliderButton).CornerRadius = UDim.new(0, 7)

	local isSliding = false
	local function UpdateSlider(inputPosition)
		local percentage = math.clamp((inputPosition.X - SliderContainer.AbsolutePosition.X) / SliderContainer.AbsoluteSize.X, 0, 1)
		SliderFill.Size = UDim2.new(percentage, 0, 1, 0)
		SliderButton.Position = UDim2.new(percentage, -7, 0, -4)
		local realValue = min + (percentage * (max - min))
		Label.Text = featureText .. " (" .. string.format("%.1f", realValue) .. "x)"
		callback(realValue)
	end
	SliderButton.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isSliding = true end end)
	UIS.InputChanged:Connect(function(input) if isSliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then UpdateSlider(input.Position) end end)
	UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isSliding = false end end)
end

local function CreateButtonRow(page, btnText, callback)
	local Row = Instance.new("Frame")
	Row.Size = UDim2.new(1, -10, 0, 45)
	Row.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
	Row.BackgroundTransparency = 0.3
	Row.Parent = page
	Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

	local Btn = Instance.new("TextButton")
	Btn.Size = UDim2.new(1, -20, 0, 33)
	Btn.Position = UDim2.new(0, 10, 0, 6)
	Btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
	Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	Btn.Text = btnText
	Btn.Font = Enum.Font.SourceSansBold
	Btn.TextSize = 14
	Btn.Parent = Row
	Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)
	Btn.MouseButton1Click:Connect(callback)
end

-- Tải các Tab chức năng cơ bản
local MainTab = CreateTab("Main Hacks")
local StatusTab = CreateTab("Status/Configs")
local UtilityTab = CreateTab("Utility")

Tabs["Main Hacks"].Visible = true
TabButtons["Main Hacks"].BackgroundTransparency = 0.4
TabButtons["Main Hacks"].TextColor3 = Color3.fromRGB(255, 255, 255)

-- Thêm tính năng di chuyển cơ bản
CreateSectionTitle(MainTab, "Hệ thống Di chuyển")
SyncUINoclip = CreateToggleRow(MainTab, "Xuyên tường (Noclip)", false, function(state) noclip = state end)
SyncUIFly = CreateToggleRow(MainTab, "Bay tự do (WASD)", false, function(state)
	if not char or not char:FindFirstChild("HumanoidRootPart") then return end
	flying = state; local hrp = char.HumanoidRootPart
	if flying then
		pcall(function()
			flyBodyGyro = Instance.new("BodyGyro")
			flyBodyGyro.P = 9e4; flyBodyGyro.maxTorque = Vector3.new(9e9, 9e9, 9e9); flyBodyGyro.Parent = hrp
			flyBodyVelocity = Instance.new("BodyVelocity")
			flyBodyVelocity.velocity = Vector3.new(0, 0.1, 0); flyBodyVelocity.maxForce = Vector3.new(9e9, 9e9, 9e9); flyBodyVelocity.Parent = hrp
			if humanoid then humanoid.PlatformStand = true end
		end)
	else
		if flyBodyGyro then pcall(function() flyBodyGyro:Destroy() end) flyBodyGyro = nil end
		if flyBodyVelocity then pcall(function() flyBodyVelocity:Destroy() end) flyBodyVelocity = nil end
		if humanoid then humanoid.PlatformStand = false end
	end
end)
CreateToggleRow(MainTab, "Nhảy vô hạn (Infinite Jump)", false, function(state) infJump = state end)

CreateSectionTitle(StatusTab, "Cấu hình Hack")
CreateSliderRow(StatusTab, "Kích thước Menu", 0.6, 1.5, 1.0, function(value) ChangeMenuScale(value) end)
CreateInputRow(StatusTab, "Tốc độ Chạy bộ:", tostring(CustomSpeed), function(text)
	local num = tonumber(text)
	if num then CustomSpeed = num; if humanoid then pcall(function() humanoid.WalkSpeed = CustomSpeed end) end end
end)

CreateButtonRow(UtilityTab, "Vào lại game phòng cũ (Rejoin)", function() TeleportService:Teleport(game.PlaceId, player) end)

----------------------------------------------------------------
-- CÁC NÚT PHỤ KHI THU NHỎ MENU CHÍNH
----------------------------------------------------------------
local OpenMenuBtn = Instance.new("TextButton")
OpenMenuBtn.Parent = ScreenGui
OpenMenuBtn.Size = UDim2.new(0, 46, 0, 46)
OpenMenuBtn.Position = UDim2.new(0, 15, 0.4, 0)
OpenMenuBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
OpenMenuBtn.Text = "N" 
OpenMenuBtn.TextColor3 = ThemeColor
OpenMenuBtn.Font = Enum.Font.SourceSansBold
OpenMenuBtn.TextSize = 22
OpenMenuBtn.Visible = false 
Instance.new("UICorner", OpenMenuBtn).CornerRadius = UDim.new(0, 23)
ObjectsToTheme[OpenMenuBtn] = "TextColor3"
MakeDraggable(OpenMenuBtn)

-- ==================== HỆ THỐNG TIMELINE CHẠY ĐỘC LẬP (AUTO BANNER) ====================
task.spawn(function()
	-- Bước 1: Cho bảng chữ Sigma rơi xuống mượt mà từ trên đỉnh màn hình
	SigmaFrame:TweenPosition(UDim2.new(0.5, -180, 0.15, 0), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.6, true)
	
	-- Bước 2: Treo bảng chữ trên màn hình 2.5 giây cho rực rỡ
	task.wait(2.5)
	
	-- Bước 3: Làm mờ bảng chữ dần dần rồi xóa hẳn
	local fadeTween = TweenService:Create(SigmaFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = UDim2.new(0.5, -180, -0.2, 0),
		BackgroundTransparency = 1
	})
	fadeTween:Play()
	fadeTween.Completed:Connect(function() 
		SigmaFrame:Destroy() 
	end)
	
	-- Bước 4: Tự động bật mở Menu chính lên ngay lập tức!
	task.wait(0.1)
	pcall(function()
		MainMenu.Visible = true
	end)
end)

-- Các nút điều hướng ẩn hiện menu sau đó
MinimizeBtn.MouseButton1Click:Connect(function()
	MainMenu.Visible = false
	OpenMenuBtn.Visible = true
end)

OpenMenuBtn.MouseButton1Click:Connect(function()
	MainMenu.Visible = true
	OpenMenuBtn.Visible = false
end)

CloseBtn.MouseButton1Click:Connect(function()
	noclip = false; flying = false; infJump = false
	if flyBodyGyro then pcall(function() flyBodyGyro:Destroy() end) end
	if flyBodyVelocity then pcall(function() flyBodyVelocity:Destroy() end) end
	if humanoid then pcall(function() humanoid.PlatformStand = false; humanoid.WalkSpeed = 16 end) end
	ScreenGui:Destroy()
end)

-- Vòng lặp core xử lý vật lý hack
UIS.JumpRequest:Connect(function() if infJump and humanoid then pcall(function() humanoid:ChangeState("Jumping") end) end end)
RunService.Stepped:Connect(function()
	if noclip and char then
		pcall(function()
			for _, part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = false end end
		end)
	end
end)
RunService.Heartbeat:Connect(function() if humanoid and char and not flying then pcall(function() humanoid.WalkSpeed = CustomSpeed end) end end)

RunService.RenderStepped:Connect(function()
	local cam = workspace.CurrentCamera
	if flying and flyBodyVelocity and flyBodyGyro and char and char:FindFirstChild("HumanoidRootPart") then
		pcall(function()
			local moveDir = Vector3.new(0, 0, 0)
			if UIS:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
			if UIS:IsKeyDown(Enum.KeyCo
